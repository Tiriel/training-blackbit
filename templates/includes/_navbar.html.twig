{% from "includes/_macros.html.twig" import navlink %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_main_index') }}">Sensio TV+</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                {{ navlink(current, 'app_main_index', 'Home') }}
            </li>
            <li class="nav-item">
                {{ navlink(current, 'app_movie_index', 'Movie') }}
            </li>
            <li class="nav-item">
                {{ navlink(current, 'app_book_index', 'Book') }}
            </li>
            <li class="nav-item">
                {{ navlink(current, 'app_main_contact', 'Contact') }}
            </li>
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            {% if app.user %}
                <li class="nav-item">
                    {{ navlink(current, 'app_logout', 'Logout') }}
                </li>
            {% else %}
                <li class="nav-item">
                    {{ navlink(current, 'app_login', 'Login') }}
                </li>
                <li class="nav-item">
                    {{ navlink(current, 'app_register', 'Register') }}
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
